name: Release Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release-disabled:
    name: Release Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled automation
        run: |
          echo "Release automation is disabled."
          echo "Repository policy forbids tracked markdown docs, and the previous release-please flow recreated markdown artifacts."
