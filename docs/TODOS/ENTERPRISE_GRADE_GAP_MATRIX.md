# Enterprise-Grade Gap Matrix

## Purpose

This matrix captures the important gaps that are either missing from the current TODO set or not yet elevated enough.

It exists to make one rule explicit:

**Filmu should be enterprise-grade and state-of-the-art across all major areas, not only feature-parity complete with `riven-ts`.**

See also:

- [`../ENTERPRISE_GRADE_AUDIT_2026_04_09.md`](../ENTERPRISE_GRADE_AUDIT_2026_04_09.md)
- [`NEXT_IMPLEMENTATION_PRIORITIES.md`](NEXT_IMPLEMENTATION_PRIORITIES.md)
- [`OBSERVABILITY_MATURITY_MATRIX.md`](OBSERVABILITY_MATURITY_MATRIX.md)
- [`FILMUVFS_BYTE_SERVING_PLATFORM_MATRIX.md`](FILMUVFS_BYTE_SERVING_PLATFORM_MATRIX.md)

## Current Filmu vs `riven-ts` position

- The comparison baseline is the clean local `riven-ts` checkout aligned to upstream `main` at `f98cc31`.
- Filmu is already stronger in platform shape: cross-process Rust VFS sidecar architecture, Windows-native adapter support, playback governance surfaces, operator-visible OIDC/authz posture, tenant quota posture, and plugin trust/publisher policy.
- `riven-ts` still sets the bar in a few focused areas: broader plugin/package ecosystem breadth, sandboxed heavy-stage orchestration, and a more mature externalized local observability stack.
- This matrix therefore tracks the gaps that still matter after first parity, not a restart of parity planning from zero.

## Matrix

| Workstream | Current evidence | Gap | Why it matters | Priority |
| --- | --- | --- | --- | --- |
| Enterprise identity and authz | [`filmu_py/api/deps.py`](../../filmu_py/api/deps.py) now carries additive actor/tenant/role/scope request context with no implicit admin fallback, computes `effective_permissions` from persisted access-policy snapshots, validates OIDC bearer JWTs against issuer/audience/JWKS when enabled, persists delegated tenant scope plus OIDC identity fields, [`filmu_py/authz.py`](../../filmu_py/authz.py) evaluates tenant-aware authorization decisions, [`filmu_py/services/identity.py`](../../filmu_py/services/identity.py) persists tenant/principal/service-account inventory, [`filmu_py/services/access_policy.py`](../../filmu_py/services/access_policy.py) now maintains active access-policy revisions plus persisted write/approve/reject/activate workflows, actor-aware API-key rotation also rotates `api_key_id`, privileged authz checks emit allow/deny audit events, [`GET /api/v1/auth/context`](../../filmu_py/api/routes/default.py) exposes the resolved actor mapping, tenant scope, OIDC validation state, and effective permissions, [`GET /api/v1/auth/policy`](../../filmu_py/api/routes/default.py) exposes policy version/source, role grants, standard authorization probes, warnings/gaps, [`GET /api/v1/auth/policy/revisions`](../../filmu_py/api/routes/default.py) exposes revision inventory, [`GET /api/v1/auth/policy/audit`](../../filmu_py/api/routes/default.py) exposes bounded audit search, and [`GET /api/v1/operations/governance`](../../filmu_py/api/routes/default.py) exposes this slice's enterprise posture | Deepen ABAC enforcement across more resources, add IdP rollout docs, JWKS rotation operations, and broaden audit retention/search beyond the bounded local operator surface | Enterprise-grade control plane requires traceable identity and policy, not one shared backend secret plus additive headers | P0 |
| Tenancy model | [`TenantORM`](../../filmu_py/db/models.py) now exists, `tenant_id` ownership covers the main `media_items` and `item_requests` lifecycle carriers, tenant-scoped authorization reaches item/calendar/stats routes, worker context binds tenant ownership, plugin load visibility exposes tenancy posture, [`GET /api/v1/tenants/quota`](../../filmu_py/api/routes/default.py) exposes quota policy, request-intake quotas are Redis-enforced when enabled, and downstream worker enqueue pressure is now also tenant-quota-enforced in [`filmu_py/workers/tasks.py`](../../filmu_py/workers/tasks.py), but plugin execution, VFS governance, provider/playback pressure, and metrics ownership are still not fully tenant-aware | Expand tenant quotas into provider/playback pressure, plugin/VFS/control-plane attribution, and tenant-scoped observability | Surpassing `riven-ts` means Filmu can safely scale beyond single-operator deployment assumptions | P0 |
| Distributed control plane | [`filmu_py/core/event_bus.py`](../../filmu_py/core/event_bus.py) can now attach [`filmu_py/core/replay.py`](../../filmu_py/core/replay.py) as a Redis Streams replay journal with event ids, offsets, tenant id, and cross-node durable storage, that replay client now exposes consumer-group create/read/ack primitives plus delivery/ack/error observations into [`filmu_py/services/control_plane.py`](../../filmu_py/services/control_plane.py), [`GET /api/v1/operations/control-plane/subscribers`](../../filmu_py/api/routes/default.py) exposes durable subscriber ownership and resume state, [`filmu_py/core/log_stream.py`](../../filmu_py/core/log_stream.py) remains bounded in-memory, and [`GET /api/v1/operations/governance`](../../filmu_py/api/routes/default.py) exposes the configured backplane posture | Promote Redis Streams into full node coordination, failover semantics, and HA readiness above the new durable subscriber ledger | Single-process signaling is a ceiling on scale, availability, and operator confidence | P0 |
| SRE / production operations program | Strong local proof scripts exist, [`OPERATIONS_PROGRAM.md`](../OPERATIONS_PROGRAM.md) defines SLO/error-budget, rollback, backup/restore, incident, canary/rollout, and capacity policy, [`scripts/run_backup_restore_proof.ps1`](../../scripts/run_backup_restore_proof.ps1) now emits backup/restore proof artifacts, and [`GET /api/v1/operations/governance`](../../filmu_py/api/routes/default.py) exposes SRE posture | Exercise and automate SLO enforcement, rollback, backup/restore, DR proof, incident runbooks, canary/rollout policy, and capacity review cadence in real isolated environments | Enterprise-grade systems are judged by operability under failure, not only by feature breadth | P0 |
| Durable operator log pipeline | [`filmu_py/logging.py`](../../filmu_py/logging.py) now provides rotating ECS/NDJSON-style file output plus the bounded in-memory broker in [`filmu_py/core/log_stream.py`](../../filmu_py/core/log_stream.py), `/api/v1/workers/queue/history` exposes bounded queue-history summaries, [`OPERATOR_LOG_PIPELINE.md`](../OPERATOR_LOG_PIPELINE.md) defines shipper/search/replay taxonomy expectations, [`ops/vector/filmu-ndjson-vector.toml`](../../ops/vector/filmu-ndjson-vector.toml) now ships both contract-console and OpenSearch sinks, [`ops/log-pipeline/docker-compose.yml`](../../ops/log-pipeline/docker-compose.yml) provisions a local collector/search stack, [`ops/opensearch/filmu-index-template.json`](../../ops/opensearch/filmu-index-template.json) pins the index mapping contract, [`scripts/check_log_pipeline_contract.ps1`](../../scripts/check_log_pipeline_contract.ps1) validates search-contract fields, and [`GET /api/v1/operations/governance`](../../filmu_py/api/routes/default.py) now exposes log-pipeline posture as its own slice instead of mixing it with plugin/runtime isolation | Promote the repo-owned stack into environment-owned deployments, broaden trace/span adoption, and wire alerting on top of the contract proof | Enterprise operations require searchable durable logs, not only live process-local compatibility streams | P0 |
| Plugin trust and isolation | [`filmu_py/plugins/manifest.py`](../../filmu_py/plugins/manifest.py), [`filmu_py/plugins/loader.py`](../../filmu_py/plugins/loader.py), and [`filmu_py/plugins/trust.py`](../../filmu_py/plugins/trust.py) now provide host-version/policy validation plus provenance metadata, digest validation, trust-store-backed signature verification, publisher lifecycle/distribution/scope policy enforcement, minimum-trust checks, tenancy-mode governance, sandbox posture, and quarantine refusal, [`filmu_py/services/plugin_governance.py`](../../filmu_py/services/plugin_governance.py) now persists operator overrides, `/api/v1/plugins` surfaces publisher-policy/quarantine state, `/api/v1/plugins/governance` summarizes signature, publisher-policy, sandbox, tenancy, readiness, and recommended operator-action posture, `/api/v1/plugins/governance/overrides` exposes persisted quarantine/revocation rows, and `/api/v1/operations/governance` now reports plugin/runtime isolation as its own roadmap slice | Add stronger runtime sandboxing and richer artifact provenance/distribution controls above the new persisted operator workflows | Plugin extensibility becomes a liability without a trust model | P1 |
| Heavy-stage workload isolation | [`filmu_py/workers/tasks.py`](../../filmu_py/workers/tasks.py) has real stages and tenant-aware downstream queue pressure enforcement, `rank_streams` now runs RTN ranking/sorting in a bounded isolated executor for crash containment, `/api/v1/workers/queue` plus `/api/v1/workers/queue/history` expose queue pressure, and `/api/v1/operations/governance` now reports heavy-stage isolation posture explicitly | Apply the same bounded isolation treatment to parse/map/validate/index-style heavy work and add explicit memory ceilings | Current upstream `riven-ts` raised the baseline here; Filmu should exceed it, not trail it | P1 |
| Metadata/index governance | Request-time enrichment and repair exist, `/api/v1/operations/governance` now tracks the release/metadata/performance discipline slice explicitly, but indexing is not yet a first-class operator program track | Add dedicated index pipeline, reindex scheduling, provenance, confidence scoring, reconciliation audits, and drift tooling | Metadata correctness is a platform capability, not a request-time convenience | P1 |
| VFS enterprise data-plane | [`rust/filmuvfs/src/mount.rs`](../../rust/filmuvfs/src/mount.rs), [`rust/filmuvfs/src/prefetch.rs`](../../rust/filmuvfs/src/prefetch.rs), and [`rust/filmuvfs/src/telemetry.rs`](../../rust/filmuvfs/src/telemetry.rs) now cover core read-path operations, adapters, a per-handle prefetch fairness cap, explicit fairness/global-backpressure denial counters surfaced through [`/api/v1/stream/status`](../../filmu_py/api/routes/stream.py), current-generation reconnect reuse and inline-refresh deduplication in the Python gRPC bridge, plus active `session_id`/`handle_key` summaries in the Rust runtime snapshot, while `/api/v1/stream/status` derives cache/fallback/prefetch pressure ratios plus `vfs_runtime_rollout_readiness`, `vfs_runtime_rollout_reasons`, and `vfs_runtime_rollout_next_action` | Add stronger soak/chaos coverage, rollout policy per backend, broader fairness/backpressure budgets, cache correctness guarantees, and richer operator controls | FilmuVFS is the clearest chance to beat `riven-ts`; it needs platform-grade operational maturity | P1 |
| Release engineering and supply chain | [`../../.github/workflows/release.yml`](../../.github/workflows/release.yml) now defaults to read-only workflow permissions, scopes `actions: write` to the release-PR verify redispatch job, and requires PAT-authenticated release-please updates so strict required PR checks report on the merge commit instead of relying on `workflow_dispatch` fallback runs that only attach to branch heads; [`../../.github/workflows/pr-branch-hygiene.yml`](../../.github/workflows/pr-branch-hygiene.yml) now rejects reused or stale feature branches once it is on `main`, `package.json` exposes `branch:codex:new` plus `branch:hygiene` for pre-PR local branch creation/audit, `security:audit`, `security:bandit`, and `perf:bench` are wired, `/api/v1/operations/governance` exposes the combined release/metadata/performance discipline slice, and artifact-level provenance and promotion policy are still incomplete | Add SBOM/signing policy, dependency risk review, release promotion stages, artifact provenance, and upgrade/downgrade contracts | State-of-the-art systems treat delivery trust as part of product quality | P1 |
| Performance and chaos discipline | Bench/perf commands exist, the combined release/metadata/performance governance slice is now operator-visible on `/api/v1/operations/governance`, but there is still no explicit benchmark/chaos program | Define latency budgets, load-test profiles, chaos scenarios, regression thresholds, and nightly benchmark comparisons against prior builds and upstream behavior | “Fast enough locally” is not an enterprise or state-of-the-art bar | P1 |

## Immediate actions

1. Continue the OIDC validation, persisted access-policy revision, tenant-aware authorization, delegated tenant scope, actor-aware key-rotation, and tenant-persistence baseline into an operator-managed enterprise identity/tenancy/authz platform.
2. Promote the Redis Streams replay plus consumer-group baseline into a durable distributed-control-plane operating model with subscriber resume offsets and failover semantics.
3. Promote plugin trust/isolation and heavy-job sandboxing into the near-term roadmap.
4. Add an SRE/DR/release-governance track so Filmu quality is measured operationally as well as functionally.
5. Keep VFS work focused on enterprise data-plane maturity, because that is where Filmu can most clearly surpass `riven-ts`.

## Success condition

This matrix is complete only when Filmu can honestly claim all of the following:

- feature breadth at or above current `riven-ts`
- stronger platform governance than current `riven-ts`
- stronger playback/VFS architecture than current `riven-ts`
- enterprise-grade operations, security, and recovery posture
- state-of-the-art engineering discipline across backend, orchestration, plugins, and data plane
